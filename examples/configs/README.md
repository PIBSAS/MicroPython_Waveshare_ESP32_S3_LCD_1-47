# Displays Modules

The following folder contain tft_config.py modules used by the example programs to configure the display. Its already on the firmware.

Folder          | Device
--------------- | -------------------------------------------------
waveshare_esp32_s3_touch_lcd_1-47 | Waveshare ESP32-S3 Touch LCD 1.47 JD853 172x320 Display

# Button Modules

These following folders contain tft_buttons.py config modules that are used by the roids.py and pinball.py example program to configure the buttons.

Folder                             | Device
---------------------------------- | --------------------------------------------------
waveshare_esp32_s3_touch_lcd_1-47  | Generic ESP32 buttons just let for the examples programs, the display doesn't have buttons so edit to 2 external buttons.
